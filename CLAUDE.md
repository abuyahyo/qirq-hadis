# CLAUDE.md

Имом Нававий «Қирқ ҳадис» — битта файлли (single-file), build-сиз ўзбекча PWA веб-илова. Барча HTML, CSS, JS ва ҳадис матнлари `index.html` ичида сақланади.

## Лойиҳа тузилиши

```
index.html         — бутун илова (HTML + CSS + JS + 42 та ҳадис маълумоти)
manifest.json      — PWA манифест (uz, standalone, portrait)
apple-touch-icon.png  — 180×180 iOS иконка
icon-192.png          — PWA иконка
icon-512.png          — PWA иконка
CLAUDE.md          — шу файл
```

**Тур:** статик HTML. Build қилиш керак эмас, фойдаланувчи `index.html`ни браузерда тўғридан-тўғри очади ёки оддий статик хостингда деплой қилади.

## Маълумот тузилиши

42 та ҳадис `index.html` ичидаги `const QD=[...]` массивида (тахминан 260–596 қаторларда). Ҳар бир ҳадис — қаттиқ скилм бўйича:

```js
{
  "id": 1,                                      // 1..42 (изчил, тартибли)
  "title": "Сарлавҳа",                          // ўзбекча, китоб мазмунига мос
  "narrator": "Ровий разияллоҳу анҳу",          // тўлиқ исм биринчи марта
  "text": "Узбекча таржима...",                 // \n = параграф/диалог қатори
  "source": "Бухорий ва Муслим ривоятлари.",    // ёки "Муслим ривоятлари." ва ҳ.к.
  "arabic": "عَنْ ... رَضِيَ اللهُ عَنْهُ..."  // тўлиқ диакритика билан
}
```

Майдонлар номи ўзгартирилмайди — JS уларни маҳкам референс қилади (`openHadis`, `matches`, `buildGrid`, `bestSnippet`).

## Git workflow

- **Ишлайдиган бранч:** ҳар сессияга айрим, кўриниши `claude/<slug>-<id>` (масалан, `claude/add-claude-documentation-8tIkN`). Топшириқда кўрсатилган бранчга **қаттиқ** риоя қилинади.
- **Асосий бранч:** `main` — фақат PR орқали янгиланади (squash merge).
- **Цикл:** edit → commit → push → (фойдаланувчи талаб қилса) PR яратиш.
- **Бранч сиёсати:** ҳар янги бранч `main` дан бошланади. Бошқа бранчга push қилинмайди.
- **Конфликт:**
  ```bash
  git fetch origin main
  git rebase origin/main
  git push --force-with-lease
  ```
- **Push retry:** тармоқ хатоси бўлса экспоненциал orqada (2с → 4с → 8с → 16с), макс. 4 марта. Hookларни сабабсиз `--no-verify` билан четлаб ўтилмайди.

## Тестлаш

Build йўқ, automated test suite ҳам йўқ. Қўлда текширув:

```bash
# Маълумот бутунлиги (массив парс бўладими, нечта элементи бор)
node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');
const m=html.match(/const QD=(\[[\s\S]*?\]);/);const QD=eval(m[1]);
console.log('Total:',QD.length,'Last id:',QD[QD.length-1].id);"
```

- **Тил-таблар, қидирув, навигация** — браузерда қўлда синалади.
- **Скриншот:** Playwright `NODE_PATH=/opt/node22/lib/node_modules` орқали (агар керак бўлса).
- **HTML/JS syntax:** `node --check` тўғридан-тўғри ишламайди (inline JS), шунинг учун парсинг — юқоридаги eval синови билан.

## Матн таҳрирлаш конвенциялари

Манба — фойдаланувчи юборган китоб расмлари (нашр). Унга **аниқ** мослаштириш мажбур.

### Узбекча матн

| Эски | Янги |
|------|------|
| `розияллоҳу` | `разияллоҳу` |
| `соллаллоҳу алайҳи васаллам` | `саллаллоҳу алайҳи ва саллам` |
| `ривояти` (Бухорий ва Муслим) | `ривоятлари` |
| Қавсли изоҳлар `(...)` | Кўпинча олиб ташланади |
| Сарлавҳалар uppercase | Доимий мос ҳолатда сақланади (CSS `text-transform:uppercase` ишлатмайди) |

- **Диалог** (масалан 2-ҳадис, 4-ҳадис) — em-dash `—` билан янги қатор, JSON ичида `\n` орқали ажратилади. UI да `\n` → `<br>` га айланади (`openHadis`да).
- **Цитата** ўзбекчада `«...»` форматда.
- **Изоҳли қавс**: китоб версиясида қавс олиб ташланган бўлса — биз ҳам олиб ташлаймиз. Лекин баъзи талаффуз қавслари (`(нутфа)`, `(алақа)`) сақланиши мумкин.
- **Кенг таржима фарқлари** бўлганда **китоб версияси устуворлик** олади.

### Арабча матн

- `ﷺ` → `صَلَّى اللهُ عَلَيْهِ وَسَلَّمَ` тўлиқ ёзилади.
- Арабча матнда `«...»` **ишлатилмайди** (тинқо/нуқтасиз ёки оддий боғловчилар билан ўз тузилмасида қолади).
- *Rawah* қисми қавсли: `(رَوَاهُ الْبُخَارِيُّ وَمُسْلِمٌ)`.
- Диакритика тўлиқ: фатҳа `َ`, кесра `ِ`, дамма `ُ`, шадда `ّ`, сукун `ْ`, танвин `ً ٍ ٌ` — копиялаганда йўқолмаслигига **қаттиқ** эҳтиёт бўлинг.
- `الخَطَّابِ` → `الْخَطَّابِ` (alif lam'дан кейин sukun).
- Алиф вариантлари (`أإآ`) ва `ى/ي`, `ة/ه` — қидирувда `normAr()` нормализация қилади, лекин матнни ёзиш керак бўлса оригиналга мос.

### Ровий

- Биринчи марта зикр қилинганда — **тўлиқ исм** (Амирул-мўминин Абу Ҳафс Умар ибн Хаттоб, Уммул-мўминин Ойша, Абу Абдурраҳмон Абдуллоҳ ибн Масъуд ва ҳ.к.).
- Кейинги ҳадисда **бир хил ровий** учун қисқартирилган форма (масалан, 2-ҳадис: фақат "Умар разияллоҳу анҳу"; 8-ҳадис: "Ибн Умар разияллоҳу анҳу").
- "Мўминлар онаси Умму ..." → "Уммул-мўминин ..." (арабий шакл, нашрга мос).

## Илова функциялари

Барча JS — `<script>` блокида (тахм. 259-қатордан охиригача).

### Саҳифалар (panels)

`showPanel(p)` — `'home'`, `'hadis'`, `'search'`, `'muqaddima'` дан бирини кўрсатади. Аслида `searchPage` div ҳозир ишлатилмайди — қидирув натижалари `homePage` ичидаги гридга чизилади.

### Маршрутлаш

- `#` (бўш) → бош саҳифа
- `#h/N` → N-ҳадис (1..42)
- `#muqaddima` → муаллиф муқаддимаси
- `hashchange` → `applyRoute()` чақирилади
- `routing` флаги `setHash()` ни рекурсиядан сақлайди

### Қидирув

- Input: `#searchInput`, debounce 300 ms (`sT` timeout id).
- `matches(h,q)` майдонлари: `text`, `narrator`, `title`, `source` (lowercase) + `arabic` (`normAr()` билан диакритикасиз).
- Муқаддима алиаслари: `'муқаддима муаллиф нававий кириш сўзбоши'` — буларнинг бирини қидирса муқаддима картаси юқорида чиқади.
- Натижа `result-item` lar — `bestSnippet(h,q)` энг мос матн парчасини олиб, `<mark>` билан ҳайлайтлайди.
- Бўш қидирув → бутун рўйхат.

### Тил-таблар

- `[Ўзбекча | Арабча]` — фақат битта матн кўринади.
- `localStorage 'lang'` (`'uz'` | `'ar'`), default `'uz'`.
- Эски ключ `showArabic='1'` → `lang='ar'` га миграция (бир мартагина).
- Агар ҳадисда `arabic` бўш бўлса, таблар яширинади.

### Клавиатура навигацияси

- `←` / `→` — олдинги/кейинги ҳадис (фақат ҳадис саҳифасида, input focusда эмас).
- `Esc` — бош саҳифа.

### Тема

- `[🌙]` тугмаси (`.theme-toggle`, чап пастда) — dark ↔ light.
- `localStorage 'th'`, default `'dark'`.
- `<html class="dark|light">` — CSS variableларни алмаштиради.

### Bottom-bar (fixed)

- 🌙 (чап пастда, айлана)
- `← ⌂ →` (марказда, pill — фақат ҳадис саҳифасида)
- ↑ scroll-top (ўнгда, айлана, scrollY > 400 бўлганда)

### Хавфсизлик

- Барча user-визибл матн `esc()` орқали escape қилинади (`&<>"'`).
- `innerHTML` ишлатилади, лекин фақат `esc()` дан кейин. `<mark>` ва `<br>` шу қоидадан кейин қўйилади.
- Search regex `q` ҳам escape қилинади (`replace(/[.*+?^${}()|[\]\\]/g,'\\$&')`).

## Дизайн қарорлари

### Палитра (CSS variables)

```css
/* Dark (default, body.html:not(.light)) */
--gold: #c9a84c;  --gold-light: #e8d08a;  --gold-dark: #8b6914;
--bg: #0d0b07;    --surface: #1c1710;     --surface2: #2d2718;
--text: #e8dcc8;  --text-dim: #9a8f78;    --text-muted: #5a5040;

/* Light (.light class) */
--bg: #f5f0e8;    --surface: #ede6d6;     --surface2: #e0d8c4;
--gold: #8b6914;  --text: #2a2218;        --text-dim: #5a5040;
```

### Layout

- **Top (home):** Бош сарлавҳа (`«Қирқ ҳадис» тўплами`) + bismillah-style hero + қидирув input + ҳадислар грид рўйхати + муқаддима карта.
- **Карта (.hadis-card):** inline формат — `1. Сарлавҳа` (рақам олтин), italic preview (text дан 140 та белги), narrator йўқ.
- **Хадис саҳифа:** сарлавҳа марказда (`hadis-pg-title`) → "1-ҲАДИС" (`hadis-pg-num`) → `[Ўзбекча | Арабча]` сегмент-таб → матн → "Ривояти: …" → "Манба: …".
- **Bottom-bar:** 🌙 + `← ⌂ →` (`hadis-nav-wrap`, fixed) + ↑.

### Шрифт

- **UI:** Roboto (sans-serif), Google Fonts.
- **Decorative сарлавҳа/footer:** Cormorant Garamond italic.
- **Арабча:** Scheherazade New (primary), Amiri (fallback).
- **Mono (label/sub):** IBM Plex Mono (CSS да rely қилади, лекин Google Fonts'дан linkланмаган — system fallback).
- **Bismillah:** Amiri.

## Иконкалар

`apple-touch-icon.png` (180×180), `icon-192.png`, `icon-512.png` — олдиндан тайёрланган.

**Дизайн:** "Қирқ" / "ҳадис" устма-уст, Cormorant Garamond italic, олтин текст cream фонда.

Қайта генерация керак бўлса, `.gen-icons.js` (Node + canvas) ишлатилади. **Ишлатилгандан кейин ўчирилади** — repoга commit қилинмайди.

## Таҳрир ҳолати

Ҳозиргача китоб нашрига мослаштирилган ҳадислар: **1–38**.

Кейинги (қилинмаган): **39–42**. Қолган ҳадислар ҳозирча эски/бошқа таржимада, шу учун ровий ёзилишида `розияллоҳу` каби эски формалар учрайди.

## Эслатмалар

- **Браузер кэши қаттиқ** — фойдаланувчи `Ctrl+Shift+R` ёки incognito билан текширсин.
- **Диакритика** жуда кўп — копиялашда (Termux/мобил буфер) йўқолмаслигига эҳтиёт бўлинг. Edit/sed емас, **JSON-aware** редактор ишлатилади.
- **Generated файллар** — `.gen-icons.js`, `.shot.js`, тест `*.png` лар коммитдан олдин ўчирилади. `.git/info/exclude` ёки мавжуд `.gitignore` йўқ — қўлда тозалаш керак.
- **JSON структураси** ўзгартирилмайди (id, title, narrator, text, source, arabic). Янги майдон қўшилса, JS ҳам янгиланиши керак.
- **`text-indent` ва паттернлар** — баъзи CSS қоидалари (масалан `text-indent:0`) inline стилда учрайди (муқаддима охиридаги дуо). Уларни алмаштирмаслик.
- **PWA test:** манифест `start_url`, `scope` ./'дан бошланади, шу учун repo root'дан хостинг қилинса ишлайди.
